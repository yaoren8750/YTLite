name: Build YouTube Plus dependencies

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      # 只保留 checkout、依赖安装、clone、build 等相关步骤
      # 删除和 ipa 构建、上传相关的所有步骤
